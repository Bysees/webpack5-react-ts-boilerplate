{ 
  //? Этот конфиг нужен для того чтобы eslint ссылался на него, а не основной конфиг (tsconfig).
  //? В противном случае могут возникать ошибки
  "extends": "./tsconfig.json",
  "include": ["**/*.*"]
}
